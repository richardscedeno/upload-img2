import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jcedenio.uploadImage',
  appName: 'uploadImage',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
